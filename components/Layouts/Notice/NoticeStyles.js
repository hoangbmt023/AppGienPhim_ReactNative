import { StyleSheet} from "react-native";

const styles = StyleSheet.create({
    notice:{
        backgroundColor: '#222',
        padding: 15,
        paddingBottom: 10,
        borderRadius: 5,
    },
    text:{
        color: 'white',
        marginBottom: 5
    },
});

export default styles;