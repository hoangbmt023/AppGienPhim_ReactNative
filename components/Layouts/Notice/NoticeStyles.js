import { StyleSheet, Text } from "react-native";

const styles = StyleSheet.create({
    notice:{
        backgroundColor: '#222',
        padding: 15,
        borderRadius: 5,
        marginTop: 10,
    },
    text:{
        color: 'white',
    },
});

export default styles;